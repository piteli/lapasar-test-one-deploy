import { ConvertToPricePipe } from './convert-to-price.pipe';

describe('ConvertToPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToPricePipe();
    expect(pipe).toBeTruthy();
  });
});
